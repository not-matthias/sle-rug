form taxOfficeExample { 
  "condition?"
    condition: boolean

  "computed condition?"
    computedCondition: boolean = condition

  if (condition) {
    "What was the selling price?"
      sellingPrice: integer

    "Private debts for the sold house:"
      privateDebt: integer
  }
}